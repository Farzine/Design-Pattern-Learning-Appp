
{
    "port": "${PORT}",
    "mongoURI": "${MONGO_URI}",
    "jwtSecret": "${SECRET_KEY}",
    "rateLimit": {
      "windowMs": "${RATE_LIMIT_WINDOW_MS}",
      "max": "${RATE_LIMIT_MAX}"
    }
  }
  